# ADR 0004 (stub)

**Status:** title only — **not** an accepted ADR. The AGENTS.md rank-1 ADR slot
is vacant until recovered ADR text exists.

**Title (from MANIFEST.md):** Decision for versioned configuration,
deterministic compilation, and reconciliation.

**Matching non-negotiable / AGENTS rule:** Configuration is compiled
deterministically in `internal/config` without connecting to LDAP. Breaking
configuration changes require a new `apiVersion`.

Do not invent rejected options, consequences, or full ADR body text. Recovered
ADR text replaces this stub and then occupies the AGENTS rank-1 slot.
