# ADR 0005 (stub)

**Status:** title only — **not** an accepted ADR. The AGENTS.md rank-1 ADR slot
is vacant until recovered ADR text exists.

**Title (from MANIFEST.md):** Decision to support scoped static bearer tokens
as an explicit lab authorization mode.

**Matching non-negotiable:** Static bearer tokens are supported as an explicit
lab mode. The design leaves room for OAuth-compatible MCP authorization later.

Do not invent rejected options, consequences, or full ADR body text. Recovered
ADR text replaces this stub and then occupies the AGENTS rank-1 slot.
