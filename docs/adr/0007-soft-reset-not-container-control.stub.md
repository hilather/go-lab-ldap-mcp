# ADR 0007 (stub)

**Status:** title only — **not** an accepted ADR. The AGENTS.md rank-1 ADR slot
is vacant until recovered ADR text exists.

**Title (from MANIFEST.md):** Decision to expose suffix-scoped reset without
container-runtime privileges.

**Matching non-negotiable:** A runtime reset is a soft data reset of the
managed suffix. A full engine reset is a deployment operation. The control
plane must not mount the Docker socket.

Do not invent rejected options, consequences, or full ADR body text. Recovered
ADR text replaces this stub and then occupies the AGENTS rank-1 slot.
