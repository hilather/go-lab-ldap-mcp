apiVersion: labldap.dev/v1alpha1
kind: LabScenario
metadata:
  name: empty-group
spec:
  directory:
    suffix: dc=example,dc=test
  transport:
    ldaps:
      enabled: true
      port: 3636
  runtimeAccount:
    id: rt
    passwordFile: secrets/runtime-ldap
  groups:
    - id: nobody
      members: []
